<h1>New comment<h1>
  <%= form_for([@blog, @comment]) do |f| %>
    <%= f.hidden_field :user_id, :value => current_user.id %>

    <%= f.label 'Comment' %>
    <%= f.text_area :content %>

    <%= f.submit %>

  <% end %>